const ApiResponse = require('../../utils/ApiResponse'); const asyncHandler = require('../../utils/asyncHandler'); const service = require('./analytics.service');
const orchestrator = require('../../ai/orchestrator/aiOrchestrator'); const aiUsage = require('../../services/aiUsage.service');
const section = (name) => asyncHandler(async (req,res) => new ApiResponse(200, await service.get(name, req.query, req.user), `${name} analytics retrieved`).send(res));
const report=asyncHandler(async(req,res)=>{const range=req.body.range||'last30Days';const started=Date.now();let result;try{result=await orchestrator.chat({message:`Generate a concise hospital performance analytics report for ${range}. Use only retrieved metrics. Include key metrics and factual observations. Include operational concerns or recommendations only when the retrieved data directly supports them. Treat zero as no matching records, not evidence of a broken workflow. If activity is insufficient, say that trends and performance cannot be assessed and suggest verifying the period and data completeness.`,requestingUser:req.user});await aiUsage.record({user:req.user,result,durationMs:Date.now()-started,success:true});return new ApiResponse(200,result,'AI analytics report generated').send(res);}catch(error){await aiUsage.record({user:req.user,result:null,durationMs:Date.now()-started,success:false});throw error;}});
module.exports = { section, report };
